#include <stdio.h>

int main() {

    /*
    arithmetic operators

    + (addition)
    - (subtraction)
    * (multiplication)
    / (division)
    % (modulus)
    ++ (increment)
    -- (decrement)
    */

    int x = 5;
    int y = 2;

    // float z = x / y;
    // x++
    // y--
    int z = x % y;

    printf("%d", z);

    return 0;
}
